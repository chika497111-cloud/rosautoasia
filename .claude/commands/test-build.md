Запусти полную проверку проекта ROSAutoAsia:

1. `npm run lint` — проверка линтером
2. `npm run build` — сборка проекта
3. Если есть ошибки — исправь их и запусти заново
4. Выведи итоговый статус: что прошло, что упало, что исправил
